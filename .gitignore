```
# SQL query files - assuming generated/intermediate files
*.sql

# If this is a database project, you might want to be more specific
# For now, treating as potential build artifact since it follows pattern of generated files
```