Nothing should be ignored in this case, as the only added file is a Solidity source code file (.sol) which is legitimate source code that should be tracked in version control. There are no build artifacts, dependencies, or temporary files in the change list that would warrant adding to .gitignore.