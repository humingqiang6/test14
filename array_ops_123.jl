# array_ops_123.jl: Basic array operations in Julia

# Define an example array
arr = [1, 2, 3, 4, 5]

# Calculate the sum
sum_result = sum(arr)
println("Sum of the array: $sum_result")

# Calculate the mean
mean_result = mean(arr)
println("Mean of the array: $mean_result")