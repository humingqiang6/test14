// Структура Point для хранения координат
const Point = struct {
    x: f32,
    y: f32,
};

// Пример использования
pub fn main() void {
    const p = Point{ .x = 3.14, .y = 2.71 };
    // В реальном приложении здесь могла бы быть логика вывода
}